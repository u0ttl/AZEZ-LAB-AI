import React,{useState}from'react';import Landing from'./Landing';import Workspace from'./Workspace';export default function App(){const[product,setProduct]=useState(location.hash==='#product');const open=()=>{setProduct(true);location.hash='product';scrollTo(0,0)};const close=()=>{setProduct(false);history.replaceState(null,'',location.pathname);scrollTo(0,0)};if(product)return <Workspace onExit={close}/>;return <div onClick={e=>{const el=e.target.closest('.endbtn');if(el){e.preventDefault();open()}}}><Landing/></div>}